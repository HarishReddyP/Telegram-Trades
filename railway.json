{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "bash -c 'cd backend && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}'",
    "numReplicas": 1,
    "restartPolicyMaxRetries": 10
  }
}

