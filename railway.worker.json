{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "bash -c 'cd backend && celery -A app.workers.celery_app worker -B --loglevel=info'",
    "numReplicas": 1,
    "restartPolicyMaxRetries": 10
  }
}
